const userHome = require('user-home')


const NPM_NAME = 'sporrow-server';
const DEST_DIR = path.join(userHome, `.sparrow/${NPM_NAME}`);

module.exports = function downloadServer(version = 'latest') {

}